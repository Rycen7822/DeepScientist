This `DeepXiv` card matters only for **literature-heavy work**.

Why it is here:

- if `DeepXiv` is configured, `idea` and `scout` can prefer that paper route
- if it is not configured, the system stays on the normal legacy route

So this is not just decoration. It changes where paper discovery comes from.
