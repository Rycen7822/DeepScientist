这里是 `BenchStore` 右侧的助手面板。

它不是静态说明，而是启动前的 `BenchStore Copilot`。

你通常会这样用它：

- 还没决定做哪个任务时，让它根据**当前机器配置**先推荐更适合的方向
- 已经选中一个 benchmark 时，让它结合任务、安装状态和本地路径，继续补齐启动信息

启动后，这一侧会切成实时 copilot，并把建议直接写回启动表单。

如果你手头已经有论文、说明文档或配置文件，也可以直接拖进输入框，让它一起参考。
