This action strip is the handoff point into the real launch flow.

Read the buttons like this:

- `Start` = the benchmark is ready enough to enter launch
- `Download` = bring missing local assets first
- `Reinstall` = refresh a broken or incomplete local state

The tutorial stays safe here. It only teaches you **where** the handoff happens.
